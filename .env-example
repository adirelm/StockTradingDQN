# Copy to .env (gitignored). No secrets are required to run TradeDQN —
# Yahoo Finance data is public. This file exists for optional overrides.

# Optional: override the default ticker / date range without editing config.
# TRADEDQN_TICKER=AAPL
# TRADEDQN_START=2020-01-01
# TRADEDQN_END=2023-01-01
