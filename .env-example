# Copy to .env (gitignored). No secrets are required to run TradeDQN —
# Yahoo Finance data is public, so a .env is optional.
#
# TradeDQN reads no runtime environment variables: choose the ticker and date
# range in the GUI form (or via the SDK's `prepare_data(ticker, start, end)`
# arguments), or change the defaults in `config/config.yaml`.
